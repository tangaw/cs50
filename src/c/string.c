#include <stdio.h>
#include <string.h>

int main(int arc, string argv[])
{
  char s[3] = "HI!";  // Declaring a string as an array of characters

  if (arc == 2) {
    printf("Hello, %s", argv[1]);
  }

  return 0;
}